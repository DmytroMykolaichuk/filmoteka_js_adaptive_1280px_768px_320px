const teamBtn = document.querySelector('.footer__btn');
teamBtn.addEventListener('click', clickOnTeamBtn);

let markup = '';

export async function clickOnTeamBtn(event) {
  event.preventDefault();
}
